---
description: Root-cause and fix a bug or failing test
---

Use the `debugger` subagent to investigate and fix: $ARGUMENTS. Follow the
reproduce -> isolate -> root cause -> minimal fix -> regression test -> verify
sequence and report each step briefly.
