---
description: Generate a PR description for the current changes
---

Use the `pr-summary` subagent to write a PR description for the current diff
($ARGUMENTS if a specific range/base branch is given). Keep it under ~200
words following the standard structure.
