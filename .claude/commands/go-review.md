---
description: Run the code-reviewer subagent over recent changes (or a given path)
---

Use the `code-reviewer` subagent to review $ARGUMENTS (if no path given, review
the current uncommitted diff via `git diff`). Report findings in the standard
review format.
