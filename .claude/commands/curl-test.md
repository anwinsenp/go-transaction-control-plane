---
description: Generate a curl-based shell test script for the ingestion service's REST API
---

Use the `curl-tester` subagent to build a shell script of curl commands
exercising $ARGUMENTS (the endpoint(s) in question, from `internal/api`).
Show the plan first, wait for approval, then write the script and tell me
how to run it against both localhost and the public sandbox URL.
