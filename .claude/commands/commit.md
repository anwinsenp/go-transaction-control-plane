---
description: Write a scoped commit message for the current changes and commit
---

Use the `commit-writer` subagent to write a commit message for $ARGUMENTS
(if no argument given, use the current staged/unstaged diff) and commit it.
