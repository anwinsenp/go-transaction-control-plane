---
description: Sketch an approach (types/signatures/steps) before writing implementation code
---

Before writing any implementation for $ARGUMENTS: restate the problem and
constraints in 2-3 sentences, list any assumptions you're making, then
propose the key types/function signatures and the order you'll build them
in. Wait for a go-ahead only if explicitly asked; otherwise proceed straight
into implementation after the sketch.
