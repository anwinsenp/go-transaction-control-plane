---
description: Break requirements/a spec into GitHub issues and create them via gh CLI
---

Use the `issue-planner` subagent to break down $ARGUMENTS (the requirements,
spec, or plan — paste inline or reference a file) into scoped GitHub issues
for this repo. Show the full plan first, wait for approval, then create the
issues with `gh issue create` and report the created issue numbers/URLs.
